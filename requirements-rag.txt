# Milestone 2 启用课程资料解析与 RAG 时安装
-r requirements.txt

anthropic==0.40.0
chromadb==0.5.23
# Chroma 0.5.x still calls the positional capture API removed by newer PostHog clients.
posthog==3.7.5
PyMuPDF==1.25.1
